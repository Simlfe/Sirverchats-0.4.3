/**
 * Lightweight application version shared by the shell and the updater.
 * Keeping this value separate prevents the browser shell from importing the
 * full update service just to render the version watermark.
 */
export const CURRENT_APP_VERSION = '0.4.3';
